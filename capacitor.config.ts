import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'uva3-1exercise',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
